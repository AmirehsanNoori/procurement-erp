{
  "name": "@lumentra/core-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Lumentra One — Core Platform contracts (module registry, core services, reporting, AI capability, workflow/approval). Type-only; no runtime, no business logic.",
  "type": "module",
  "types": "src/index.ts",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
